# Supabase Configuration

This directory contains Supabase-specific configuration files.

## Files

- **docker-compose.override.public.supabase.yml** - Supabase-specific overrides for public/production environment

## Usage

This file is automatically referenced by `start_services.py` when using the public environment.

