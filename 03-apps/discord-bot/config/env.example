# Discord Bot Configuration
# Copy this file to your .env file and fill in the values

# Discord Bot Token
# Get this from: https://discord.com/developers/applications
# Create a new application → Bot → Create Bot → Copy Token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Discord Channel ID for file uploads (optional)
# Enable Developer Mode in Discord → Right-click #event-uploads → Copy ID
# Leave empty to allow uploads from any channel
DISCORD_UPLOAD_CHANNEL_ID=

# Lambda API Configuration (for user-specific Immich uploads)
# Lambda server URL (internal Docker network)
LAMBDA_API_URL=http://lambda-server:8000

# Lambda API Token (optional but recommended for security)
# Generate via: POST /api/v1/auth/me/token after authenticating via Cloudflare Access
# This allows the bot to authenticate even if network topology changes
LAMBDA_API_TOKEN=

# Cloudflare email (fallback for internal network auth if LAMBDA_API_TOKEN not set)
CLOUDFLARE_EMAIL=

# Immich Configuration
# Immich server URL (internal Docker network)
IMMICH_SERVER_URL=http://immich-server:2283

# Immich Admin API Key
# Get this from: Immich Web UI → Settings → API Keys → Create API Key
IMMICH_API_KEY=your_immich_api_key_here

# Database Path (default, usually no need to change)
BOT_DB_PATH=/app/data/bot.sqlite

# Notification Polling Interval (seconds)
# How often to check for new assets (default: 120 = 2 minutes)
NOTIFICATION_POLL_INTERVAL=120

# MCP Server Configuration (optional)
# Enable MCP server for programmatic Discord access
MCP_ENABLED=true
MCP_PORT=8001
MCP_HOST=0.0.0.0
